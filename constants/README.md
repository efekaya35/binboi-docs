# `constants`

Purpose: structured product content, navigation definitions, and static dashboard/public copy.

What belongs here:
- Marketing copy, docs article models, navigation arrays, and other stable configuration-like data.
- Shared product metadata such as name, tagline, and public descriptions.

Notes:
- Keep real backend contracts out of this folder; they belong in `lib/backend`.
- Structured content here powers the docs IA and multiple public pages.
